{
  "name": "chat-socket-server",
  "version": "1.0.0",
  "description": "Standalone Socket.IO server for real-time chat",
  "main": "socket-server.js",
  "scripts": {
    "start": "node socket-server.js",
    "dev": "nodemon socket-server.js",
    "build": "echo 'No build step required for Socket.IO server'",
    "postinstall": "echo 'Socket.IO server dependencies installed'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
